# Legacy Material

`legacy/clubelo/` contains the original ClubElo-era scripts kept as reference.

`legacy/misc/` contains unrelated historical football analysis, plots, notebooks, and source files from the old folder. It is intentionally excluded from Git via `.gitignore` so the maintained repository stays focused on the current ClubElo pipeline.
